{% extends 'ProfileBundle:Layout:index2.html.twig' %}

{% block title %}Boi{% endblock %}
{% block page_css %}

    <style>
        html, body{
            background: #18253f;
        }
        .body-content{
            height:100%;
        }
        /* NAVIGATION */
        nav {
            width: 100%;
            margin: 0 auto;
            background: #131e33;
            padding: 10px 0;
        }
        nav ul {
            list-style: none;
            text-align: center;
            margin-top: 0;
            margin-bottom: 0;
        }
        nav ul li {
            display: inline-block;
        }
        nav ul li a {
            display: block;
            padding: 15px;
            text-decoration: none;
            color: #aaa;
            font-weight: 800;
            text-transform: uppercase;
            margin: 0 10px;
        }
        nav ul li a,
        nav ul li a:after,
        nav ul li a:before {
            transition: all .5s;
        }
        nav ul li a:hover {
            color: #555;
            text-decoration: none;
        }
        /* SHIFT */
        nav.shift ul li a {
            position:relative;
            z-index: 1;
        }        


        /* Brackets go out */
        nav.shift a:before, nav.shift a:after
        {
            position: absolute;
            opacity: 0;
            font-size: 35px;
            top: 0px;
            transition: all 0.3s;
        }
        nav.shift li#profile_picture:after {
            content: "TEST";
            transform: translateX(-10px);
            left: 0px;
            opacity:0;
        }

        nav.shift a:before
        {
            content: '(';
            left: 0px;
            transform: translateX(10px);
        }

        nav.shift a:after
        {
            content: ')';
            right: 0px;
            transform: translateX(-10px);
        }

        nav.shift a:hover:before, nav.shift a:hover:after
        {
            opacity: 1;
            transform: translateX(0px);
        }
        nav.shift li#profile_picture:hover:after {
            opacity: 1;
            transform: translateX(0px);
        }
    </style>
{% endblock page_css %}